require 'spec_helper'

describe ToyRobotSim do
  it 'has a version number' do
    expect(ToyRobotSim::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
